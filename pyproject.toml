[project]
name = "llm-from-scratch"
version = "0.1.0"
description = "Working through 'Build an LLM from Scratch'"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "tiktoken",
]
